<Window x:Class="PartMap.Dialogs.PromptDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="输入" Width="460" SizeToContent="Height"
        ResizeMode="NoResize" WindowStartupLocation="CenterOwner">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock x:Name="PromptText" FontSize="14" Margin="0,0,0,8" TextWrapping="Wrap" />
        <TextBox x:Name="ValueTextBox" Grid.Row="1" MinWidth="390"
                 KeyDown="ValueTextBox_OnKeyDown" />
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="取消" IsCancel="True" />
            <Button Content="确定" IsDefault="True" Click="Accept_OnClick" />
        </StackPanel>
    </Grid>
</Window>
