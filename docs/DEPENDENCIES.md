# Dependencies

PartMap targets .NET 8 and uses Windows WPF from the .NET SDK. The repository does not currently require third-party NuGet packages for the application itself.

Optional slicing integration invokes an existing Bambu Studio installation on the user's machine. Bambu Studio is maintained and licensed separately.
